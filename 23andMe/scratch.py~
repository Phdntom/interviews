def aligned_lists(dict1, dict2):
    '''
    '''

    x = []
    y1 = []
    y2 = []
    for key in dict1:
        x.append(key)
        y1.append(dict1[key])
        y2.append(dict2[key])

    data = zip(x,y1,y2)
    data.sort(key=lambda t: t[0])
    x, y1, y2 = list(zip(*data))

    return x, y1, y2


class change_dir():
    '''
    '''
    def __init__(self, newPath):
        self.newPath = newPath

    def __enter__(self):
        self.savedPath = os.getcwd()
        os.chdir(self.newPath)

    def __exit__(self, etype, value, traceback):
        os.chdir(self.savedPath)


    if more_plots is not None:
        colors = ['b', 'r', 'g', 'c', 'm']
        colorcycler = cycle(colors)
        for each in more_plots:
            ax.scatter(x,each, c=next(colorcycler) )
